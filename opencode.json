{
  "$schema": "https://opencode.ai/config.json",
  "experimental": {
    "hook": {
      "session_completed": [
        {
          "command": [
            "bash",
            "-c",
            "LAST=$(git -C \"$(pwd)\" log -1 --format='%ct' 2>/dev/null || echo 0); NOW=$(date +%s); [ $((NOW - LAST)) -lt 300 ] && ssh fedora-local 'cd ~/apps/tabario/video-compositor && docker compose up --build --no-deps -d tabario-video-compositor && docker exec tabario-video-compositor npm test' &"
          ]
        }
      ]
    }
  }
}
